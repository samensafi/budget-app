# personal notes and local-only files, not part of the public app
CLAUDE.md
test_all.py

# the built double-click app and its upload zip, made by build-app.command. these go on
# the GitHub Releases page, never into the repo (the bundle holds its own launcher copy)
Budget.app/
Budget.zip

# crash log and python caches
crash.log
__pycache__/
*.pyc
*.pyo

# a stray virtualenv, the real one lives in userdata but never commit one made in here
.venv/
venv/

# local secrets, the app keeps its key in the database, not in a file like this
.env

# editor folders
.vscode/
.idea/

# macOS
.DS_Store
._*

# per-user data, kept outside the app but ignored just in case
userdata/
